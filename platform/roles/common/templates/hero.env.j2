# Hero Environment Configuration
# Generated on {{ ansible_date_time.iso8601 }}

# Network Configuration
MAIN_NETWORK={{ main_network | default('wireguard') }}
NETWORK_MODE={{ network_mode | default('both') }}

# Domain Configuration
DOMAIN_NAME={{ domain_name | default('hero.projectmycelium.org') }}

# Redis Configuration
REDIS_PASSWORD={{ redis_password }}

# Hero Backend Configuration
HERO_BACKEND_PORT={{ hero_backend_port | default(8080) }}

# UI Colab Configuration
UI_COLAB_PORT={{ ui_colab_port | default(3000) }}

# SSL Configuration
ENABLE_SSL={{ enable_ssl | default('false') }}
SSL_EMAIL={{ ssl_email | default('admin@' + domain_name | default('hero.projectmycelium.org')) }}
SSL_STAGING={{ ssl_staging | default('false') }}